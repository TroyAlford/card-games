{
  "name": "card-games",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server",
    "client",
    "games/*"
  ],
  "scripts": {
    "develop": "concurrently -n server,client -c 'bgGreen.bold,bgBlue.bold' \"bun run dev:server\" \"bun run dev:client\"",
    "dev:server": "bun run --cwd server develop",
    "dev:client": "bun run --cwd client develop",
    "build": "bun run --cwd client build && bun run --cwd server build"
  },
  "devDependencies": {
    "chokidar": "^4.0.3",
    "concurrently": "^9.1.0",
    "sass": "^1.83.0"
  },
  "dependencies": {
    "basis": "TroyAlford/basis"
  }
}