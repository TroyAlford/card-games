{
  "name": "@card-games/server",
  "version": "1.0.0",
  "scripts": {
    "develop": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
} 